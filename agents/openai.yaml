interface:
  display_name: "Daily Chitchat"
  short_description: "English practice with scenario checks and a review queue"
  default_prompt: "Use $daily-chitchat and start our daily session."
policy:
  allow_implicit_invocation: true
